public class Receptionist extends User {

    public Receptionist(String id, String name, String password) {
        super(id, name, password);
    }

    // Receptionist-specific actions can be added later
}
